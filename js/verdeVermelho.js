function alteraCorDeFundo(vCor) {
    document.body.style.backgroundColor = vCor;
    /*
    Função para alterar a cor de fundo
    aplicando a cor que for informada
    no parâmetro da chamada da função.
    */
}